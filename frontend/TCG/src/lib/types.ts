export interface card {
    id: number;
    damage: number;
    effect: string;
    name: string;
 }

export interface classCard{
   className:string,
   description: string
}
 
 export interface cardData {
   damage: number;
   effect: string;
   name: string;
}

